# Type safety
// ... with full-stack type safety (optional)
import type { server } from "../../server/src/app.config.ts";
const client = new Client<typeof server>("http://localhost:2567");

# Use Colyseus SDK better

https://docs.colyseus.io/room/built-in/lobby for lobby


# Deploy backend in Vercel and frontend in Github Pages

# load testing https://docs.colyseus.io/tools/loadtest

# Explore DBs https://docs.colyseus.io/database

# Add OAuth with Google or Github